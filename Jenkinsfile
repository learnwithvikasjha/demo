pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        shell('echo this is a build stage')
      }
    }
      
       stage('Test') {
      steps {
        shell ('sh command.sh')
      }
       }
       
      }
}
