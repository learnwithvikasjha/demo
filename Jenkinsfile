pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        shell('echo this is a build stage')
      }
    }
      
       stage('Test') {
      steps {
        shell ('echo "This is a test stage."')
      }
       }
       
      }
}
